<Window x:Class="Clamor.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Clamor.App.Views"
        Title="Clamor" Height="650" Width="900" MinWidth="640" MinHeight="420"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <Grid Margin="16,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="pack://application:,,,/Assets/clamor-text-cropped.png"
                           Height="22" Stretch="Uniform" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Mic passthrough" Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ToggleButton Style="{StaticResource ToggleSwitchStyle}"
                                  IsChecked="{Binding MicPassthroughEnabled, Mode=TwoWay}"
                                  VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <Button Content="Stop All" Command="{Binding StopAllCommand}" Margin="0,0,8,0"/>
                    <Button Content="Add Clip" Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding AddClipCommand}" Margin="0,0,8,0"/>
                    <Button Content="&#9881;" Style="{StaticResource IconButtonStyle}"
                            Command="{Binding OpenSettingsCommand}" ToolTip="Settings" Margin="0,0,12,0"/>
                    <Button Content="&#xE921;" Style="{StaticResource TitleBarButtonStyle}" Click="Minimize_Click"/>
                    <Button x:Name="MaximizeRestoreButton" Content="&#xE922;" Style="{StaticResource TitleBarButtonStyle}"
                            Click="MaximizeRestore_Click"/>
                    <Button Content="&#xE8BB;" Style="{StaticResource TitleBarCloseButtonStyle}" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource SurfaceAltBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="16,10">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal">
                        <ItemsControl ItemsSource="{Binding ProfileTabs}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource ProfileTabButtonStyle}" Margin="0,0,8,0"
                                            Command="{Binding DataContext.SelectProfileCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding Name}"
                                            AllowDrop="True"
                                            PreviewMouseLeftButtonDown="ProfileTab_PreviewMouseLeftButtonDown"
                                            PreviewMouseMove="ProfileTab_PreviewMouseMove"
                                            Drop="ProfileTab_Drop">
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="6" Height="6" Margin="0,0,6,0" VerticalAlignment="Center"
                                                     Fill="{StaticResource AccentBrush}"
                                                     Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            <TextBlock Text="{Binding Name}"/>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Button Content="+" Style="{StaticResource IconButtonStyle}"
                                Command="{Binding AddProfileCommand}" ToolTip="New profile"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource SurfaceAltBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="16,10">
                <Button Content="Set Active" Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding ActivateViewedProfileCommand}" Margin="0,0,8,0"/>
                <Button Content="Delete Profile" Style="{StaticResource DangerButtonStyle}"
                        Command="{Binding DeleteViewedProfileCommand}"
                        Visibility="{Binding IsViewingInactiveProfile, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

        <local:SoundGridView Grid.Row="3"/>
    </Grid>
</Window>
