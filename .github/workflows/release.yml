name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: windows-latest
    # softprops/action-gh-release needs write access to create the release, upload the
    # installer, and generate release notes — the default GITHUB_TOKEN is read-only unless
    # granted this explicitly.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      # Re-run the exact same build+test gate as PR validation rather than trusting a prior
      # green check — what ships is built and tested fresh from the tagged commit.
      - name: Restore
        run: dotnet restore Clamor.slnx

      - name: Build
        run: dotnet build Clamor.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test Clamor.slnx --configuration Release --no-build --verbosity normal

      - name: Determine version from tag
        id: version
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}" -replace '^v', ''
          "version=$version" >> $env:GITHUB_OUTPUT

      - name: Publish app (self-contained win-x64)
        run: >
          dotnet publish src/Clamor.App/Clamor.App.csproj
          --configuration Release
          --runtime win-x64
          --self-contained true
          -p:PublishSingleFile=false
          -p:Version=${{ steps.version.outputs.version }}
          -o publish

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Build installer
        run: iscc /DMyAppVersion=${{ steps.version.outputs.version }} installer\Clamor.iss
        shell: cmd

      - name: Upload release artifact
        uses: softprops/action-gh-release@v2
        with:
          files: installer/Output/*.exe
          generate_release_notes: true
